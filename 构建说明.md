# 川小熊猫 v1.0.1 构建说明

## 构建结果

成功构建了川小熊猫软件的安装版和便携版：

1. **安装版**：`dist/川小熊猫安装版-1.0.1.exe` (约76.0 MB)
   - 提供标准Windows安装程序
   - 支持自定义安装目录
   - 自动创建桌面快捷方式和开始菜单项
   - 支持卸载功能

2. **便携版**：`dist/川小熊猫便携版-1.0.1.exe` (约75.8 MB)
   - 无需安装，直接运行
   - 可放置在U盘等移动设备上使用
   - 不修改系统注册表

## 构建过程

使用Electron-builder工具进行构建，具体步骤如下：

1. 安装依赖：`npm install`
2. 构建便携版：`npm run dist:portable`
3. 构建安装版：`npm run dist:installer`

## 构建配置

构建配置位于package.json文件的"build"部分，主要包括：

```json
"build": {
  "appId": "com.chuanpanda.app",
  "productName": "川小熊猫",
  "directories": {
    "output": "dist"
  },
  "win": {
    "target": [
      {
        "target": "nsis",
        "arch": ["x64"]
      },
      {
        "target": "portable",
        "arch": ["x64"]
      }
    ],
    "icon": "42-ico.ico"
  },
  "nsis": {
    "oneClick": false,
    "allowToChangeInstallationDirectory": true,
    "createDesktopShortcut": true,
    "createStartMenuShortcut": true,
    "shortcutName": "川小熊猫",
    "artifactName": "川小熊猫安装版-${version}.${ext}"
  },
  "portable": {
    "artifactName": "川小熊猫便携版-${version}.${ext}"
  }
}
```

## 发布说明

构建的安装版和便携版已上传至GitHub发布页面：
https://github.com/fayufm/ChuanPanda/releases/tag/v1.0.1-release

用户可以根据自己的需求选择下载安装版或便携版。 